import 'package:flutter/material.dart';

class SlidersAppInfo extends StatelessWidget {
  const SlidersAppInfo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
